
CHANGELOG

* 2016/02/02:

    - Revised. Working on Python 3.5.0 and Pillow 3.1.0.

* 2014/08/24:

    - Prefix program name on errors to stderr.
      e.g. 'EmbedDIBits.py: error: message...'

    - Emit more pixels per line (70 characters).

    - Working on Pillow 2.5.3.

* 2014/07/21:

    - Revised. Working on Python 3.4.1 and Pillow 2.5.1.
      No changes needed.

* 2013/07/31:

    - Emit the pixels with premultiplied alpha.
      Allows drawing PNGs and images with per-pixel alpha, using the AlphaBlend
      function with AC_SRC_ALPHA.

* 2013/07/30:

    - '--new' is now '--newline'.

* 2013/07/25:

    - Added '--new system'.

    - Default to system newlines instead of unix.

* 2013/07/09

    - Write in binary, always, to avoid Python automatic newline conversion.
      Fixes '--new [format]' not working when using stdout.

* 2013/07/08:

    - Avoid creating blank header files when images do not exist.

* 2013/06/20:

    - Port to Python 3 and Pillow.

